var preheatingProcess = require('./preheating-process');
const expect = require('chai').expect

describe('Preheating process', function () {
  it('it does nothing', function * () {
      
      expect(true).to.be.fatrue;
  })
})