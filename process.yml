apps:
  - script   : src/server.js
    name     : 'herms-backend'
    cwd      : './'
    exec_mode: 'fork'    
    env      :
      NODE_ENV: production	
      FRONTEND_PATH: ./
      USB_PORT: /dev/ttyACM0
      

